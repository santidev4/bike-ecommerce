import styled from 'styled-components'

export const Logo = styled.div`
    font-size: 30px;
    font-weight: 400;
    letter-spacing: 8px;

    a {
        color: rgb(30, 30, 30);
        text-decoration: none;
    }
`