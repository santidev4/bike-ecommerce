import styled from 'styled-components'

export const HeroImage = styled.img`
    width: 100%;
    padding: 20px 20px;
    border-radius: 45px;
    opacity: 0.9;
    overflow: hidden;
    position: relative;
`