export type ProductType = {
  name: string
  price: number,
  width: number,
  height: number,
  weight: number,
  stock: number,
  length: number,
  brand: string,
  img: string,
  description: string,
  categories: string[]
}

export type CategoryType = {
  name: string
}

export type BrandType = {
  name: string
}
